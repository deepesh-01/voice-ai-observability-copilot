# Functional vs Mocked — Living Ledger

> Required deliverable **D3.1**: "Notes on what is functional (real-time transcript
> ingestion) vs what is mocked." This is a living table — update it as components move from
> mocked → real. Honesty here is graded (E2, E4).

**Legend:** 🟢 Functional (real) · 🟡 Partial / behind a flag · 🔴 Mocked · ⬜ Not built yet

| Component | Traces to | State | Notes |
|-----------|-----------|-------|-------|
| HighLevel OAuth (install/callback/refresh) | R1.2 | 🟢 | **Real install completed** — sandbox sub-account `B7TzvBb6H6QvDNdEEhlt` connected; token stored + refresh wired. **Token store moved to Postgres (`oauth_tokens`, S-015):** durable + multi-tenant, replacing the gitignored `tokens.json` (kept only as a one-shot migration source via `scripts/migrate-tokens.mts`). Read-back + refresh verified live from the DB; covered by a 5-test integration suite. |
| HighLevel sandbox & marketplace app install | R1.1, R1.2 | 🟢 | App **Live (v2.0.1)**; installed on a sandbox sub-account. Adding Voice AI scopes via a new version + reinstall (A-007/A-011). |
| **Dashboard embedded inside HighLevel** | R1.2, E1 | 🟢 | **VERIFIED LIVE (2026-06-21), two surfaces.** (1) Marketplace **Custom Page** (`Voice AI Observability`, iframe → `https://voai.deepesh-engg.in/`) added to v2.0.1 and reinstalled into sub-account `B7TzvBb6H6QvDNdEEhlt` → renders as a tab on the app page. (2) Agency **Custom Menu Link** (iframe embed) → the dashboard also appears as a **left-sidebar item** in the sub-account. Both render embedded in the HighLevel chrome with real data. Enabled by the server's `frame-ancestors` CSP for `*.gohighlevel.com`/`*.leadconnectorhq.com` + no `X-Frame-Options` (`server/src/index.ts`). Closes A-001 — no Custom JS fallback needed. |
| Read-API authentication (`/api/*`) | E3, E4 | 🟢 | **Live (2026-06-21).** All `/api/*` routes require `Authorization: Bearer <API_AUTH_TOKEN>` (or `x-api-key`), constant-time compared (`server/src/middleware/apiAuth.ts`). The token is provisioned per-deploy via env and **injected into the served SPA at runtime** (`window.__API_TOKEN__`, never in git), so the embedded + standalone dashboard authenticates transparently. `/health`, `/oauth/*`, `/webhooks/*` stay open (own auth: OAuth flow, Ed25519 signature). Verified live: 401 without token / 200 with / 401 wrong; dashboard loads clean. **Honest scope:** this blocks public/automated access to call data, but anyone who loads the page can read the token — it's not per-user identity. **Next:** layer HighLevel SSO (decrypt the embedded user context with the app Shared Secret) for per-user/per-location auth. |
| Transcript ingestion (API) | R2.1, D1.1 | 🟢 | **Live, real data captured (S-012).** List + Get Call Log both live; real transcripts in `server/fixtures/` (incl. a full 123s booking call). `getCallLog` fixed to pass `locationId`. Capture tool `server/scripts/capture-call-shape.mts`. Still a **manual pull** — no webhook/poll ingestion pipeline yet (TBD). |
| Voice AI agent config (API write) | R2.2 setup | 🟢 | **Live (S-012).** Agent created in UI, then re-provisioned via `PATCH /voice-ai/agents/:id` (`Version: v3`) with `scripts/configure-agent.mts`. App carries `voice-ai-agents.write` + `voice-ai-agent-goals.write` after reinstall. Goals/actions not yet set (→ A-005). |
| Transcript parser | R2.1 | 🟢 | **Built + tested (S-012).** `analysis/transcript.ts` parses the `bot:/human:` string → typed turns (merges consecutive same-speaker lines; no timestamps). 9 unit tests; verified on real fixtures. |
| Observability parameters / KPI config | R2.2 | 🟢 | **Built (S-012).** `analysis/kpis.ts` — 6 config-driven KPIs (A-004): goal_completion, script_adherence, info_capture, accuracy, objection_handling, sentiment. Weighted overall, unit-tested. Dead-air dropped (no timestamps). |
| KPI scoring & deviation detection (LLM) | R2.3 | 🟢 | **Live, run end-to-end (S-012).** `analysis/score.ts` scores transcript+goal → KPI scores + deviations + "Use Actions" via the **Claude Agent SDK** (`claude-opus-4-8`, structured output), authed by `CLAUDE_CODE_OAUTH_TOKEN` (no bare API key). Pure logic unit-tested (7 tests); **closed the loop on the real 123s booking call** (overall 89/100, caught first-name-only + dropped-email gaps). Run: `scripts/score-fixture.mts`. |
| Unified dashboard | R2.4, R2.5, R2.6, E1 | 🟢 | **Live (S-013).** Full agent → call → flagged-segment drill-down. Cross-agent overview (agent cards + KPI strips), agent detail (KPI profile + lazy recommendations + call list), call detail (KPI scorecards + evidence chips → scroll-to-turn + transcript with UseAction banners/highlights + deviations). All real API data; no stubbed data. **Agent names resolved live from GHL (`GET /api/agents`, `agentName`) — raw ids demoted to secondary text/tooltip (S-013).** ConnectionsPanel demoted to collapsible (UX-001/UX-006 preserved). Emil craft pass (UX-007) + **Playwright E2E suite (15 tests) + vitest unit (15)** cover the loop, states, signals, and animation behavior. **Lead/observability-signal surfacing added (S-016)** — see the Lead-extraction row. |
| Recommendations engine | R2.5 | 🟢 | **Live, run end-to-end (S-013).** `analysis/recommend.ts` gathers an agent's stored analyses + KPI averages, builds a bounded evidence digest (KPI averages weakest-first + deviations grouped by KPI + per-call summaries), and synthesizes concrete prompt/script fixes via the Claude Agent SDK (Opus, per ADR-0002 routing). Pure digest/assembly logic unit-tested (7 tests); evidence call-ids filtered to real stored calls (no hallucinated references). **Closed the loop on 5 real calls** for the BrightSmile agent — produced 5 prioritized, copy-pasteable fixes (full-name gate, don't-drop-email, caught a misconfigured `Demo` greeting). `GET /api/recommendations`; run: `scripts/recommend.mts`. **Cached in Postgres keyed by agent + scored-call count (S-013): the Opus call only runs on a cache miss (new calls or `?refresh=1`) — verified live: 67s first call → 0.005s cached.** |
| Apply recommendation → agent prompt (write) | R2.5, A-012 | 🟢 | **Live write path (S-020).** Dashboard "Apply" on a recommendation → `POST /api/agents/:id/recommendations/:i/preview` runs an **Opus prompt-merge** (`analysis/applyRecommendation.ts`) to produce the complete revised prompt (no write), shown as a before/after **diff for explicit confirm**; `POST /api/agents/:id/apply` then does the **real** `PATCH /voice-ai/agents/:id` (`Version: v3`, `voice-ai-agents.write`) — same proven path as `scripts/configure-agent.mts`, not a mock. Safety: writes **serialized** by an in-process mutex (one agent update at a time — `util/mutex.ts`, 3 tests); **optimistic-concurrency baseline** rejects a stale overwrite (409); body sends **only `agentPrompt`** (merge preserves actions — S-012); read-back **verifies** the prompt took (`ok`) and the action **count** is unchanged (count-only check — a same-count replacement would pass; a drop logs a pre-write backup to the server log and returns 502). Tests: `ghl/api.test.ts` (6 — write/baseline/clobber/not-found/ok-false). **Caveat:** verified by tests + manual flow; not yet fired against the live sandbox agent in this session. |
| Persistence (raw + analyses + KPIs) | R2.1, R2.3, R2.4 | 🟢 | **Live, real Postgres (S-012, ADR-0008; normalized S-015, ADR-0011).** `raw_call` is the **source-of-record**, written on ingest arrival before scoring (so a call survives a scoring failure); `call_analysis` (derived KPI scoring) + `call_lead` FK to it; `call_kpi` flat rows for `GROUP BY` analytics. Swappable `RawCallRepository`/`AnalysisRepository`/`LeadRepository`. Real integration tests against local Postgres (55-test suite). Migration `scripts/migrate-split-raw.mts` ran on the dev DB (5 real calls); layered re-ingest verified end-to-end (raw → score → lead). |
| Ingestion pipeline (real-time flywheel) | R2.1, R2.1a | 🟢 | **Webhook (primary) live + verified (S-012).** Marketplace `VoiceAiCallEnd` → `POST /webhooks/ghl/voice-ai`: a real **web call** triggered it, **Ed25519 signature verified**, inline transcript scored + persisted in seconds. Handler acks **202 in ~0.03s** then ingests async (GHL retries slow handlers; `inFlight` set + idempotent `has()` dedupe retries). **Poll** (`scripts/ingest.mts`) is the backfill safety net. Signature enforcement opt-in via `WEBHOOK_REQUIRE_SIGNATURE`. |
| Dashboard API (read) | R2.4 | 🟢 | **Live (S-012).** `GET /api/analyses`, `/api/analyses/:callId`, `/api/kpis/averages` serve persisted data; verified returning real scored calls. |
| "Use Actions" segment flagging | R2.6 | 🟢 | **Live (S-013).** Surfaced in the call detail view: UseAction spans are highlighted as amber banners over the transcript with the `label` + `reason`; turns within the span are visually distinct. Not yet tied to `extractedData`/`executedCallActions` (A-005 open). |
| Lead extraction + observability signals | R2.1, R2.2, R2.3, R2.6, E2 | 🟢 | **Backend live + verified on 16 real calls (S-015; ADR-0010 → 0012 → hybrid 0013).** Each call → `call_lead`. **Facts** (name/phone/email/problem/treatment/booking) now come from the agent's **native GHL `extractedData`** (ground-truth — clean E.164 phone, structured `bookingInterest`, `Treatment Interest`), falling back to the contact record then the LLM; a `source` flag ('ghl'/'llm') + stored raw `native` blob keep provenance honest. **Signals** — `missed_opportunity` (R2.3) + `human_action_needed` (R2.6), each with a reason — are **always LLM** (GHL records successes, not gaps). Agent reconfigured with 9 `DATA_EXTRACTION` actions; real calls return populated `extractedData` per-call. Keyword mapping in `analysis/nativeFacts.ts` (unit-tested on the real shape); suite 59. `GET /api/leads?missedOpportunity=1&humanActionNeeded=1`. Read-only. **Surfaced across the dashboard (S-016):** Call view "Lead & Outcome" panel (booking status + facts + both signals with reasons + `source` provenance badge + expandable native `extractedData` drawer); Agent view (header signal tallies, per-row MO/HA badges, signal filter toggles); Overview (per-agent + location-wide signal counts). Wired to `GET /api/leads` + `/api/leads/:callId`; verified live against Postgres. |
| ~~Booking-approval workflow~~ | — | ⚪ | **Dropped (ADR-0012).** Briefly built (per-agent `booking_policy`, `approval_status`, approve/reject endpoints) then **removed as scope drift** — it re-implemented HighLevel's CRM and isn't observability. Replaced by the two read-only signals above. `scripts/migrate-lead-signals.mts` dropped the table/columns. |

## Rule

When a component ships, change its state and write one line of *what is real vs simulated*
(e.g. "real ingestion via webhook; KPI thresholds seeded from a default profile, not yet
learned"). No silent mocking — a faked path must say so here.
